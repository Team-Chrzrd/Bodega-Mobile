const authCookie = require('./authCookie.js');
const authSignin = require('./authSignin.js');
const authSignup = require('./authSignup.js');
const authCheckCookie = require('./authCheckCookie.js');

module.exports = {
  authCookie,
  authSignin,
  authSignup,
  authCheckCookie,
};
